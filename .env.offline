# Offline build profile. Loaded automatically by `vite ... --mode offline`.
# Forces the app into fully-local mode: no Firebase, no login wall, content and
# saved data live in the browser on this device. This is the resilient fallback
# that keeps CM Simulator working with no internet / if Firebase ever goes down.
#
# Safe to commit — contains no secrets.
VITE_OFFLINE=1
